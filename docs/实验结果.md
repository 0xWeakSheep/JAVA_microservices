# 实验结果 - Zuul网关过滤实验

## 实验信息

- **实验名称**：Zuul网关过滤实验
- **学号**：2023131016
- **姓名**：操祥睿
- **实验日期**：2025年4月11日

---

## 一、实验目的

| 序号 | 实验目的 | 完成状态 |
|------|----------|----------|
| 1 | 理解Spring Cloud Zuul网关在微服务架构中的核心作用 | ✅ 完成 |
| 2 | 掌握网关作为微服务统一入口的路由转发、权限校验等核心原理 | ✅ 完成 |
| 3 | 验证并掌握Zuul网关过滤器的实际效果 | ✅ 完成 |

---

## 二、实验内容完成情况

### 任务1：搭建Spring Boot注册中心模块(Eureka)

**实现内容**：
- 创建了 `eureka-server` 模块，端口 8761
- 添加了 `@EnableEurekaServer` 注解启用注册中心
- 配置了 Eureka Server 的自我保护模式和注册表同步

**验证结果**：

| 验证项 | 访问地址 | 结果 |
|--------|----------|------|
| Eureka Server 运行状态 | http://localhost:8761 | ✅ 正常运行 |
| Eureka 控制台显示 | http://localhost:8761 | ✅ 显示注册的服务列表 |

**截图说明**：
- 截图1：`EurekaServerApplication.java` 代码（展示 `@EnableEurekaServer` 注解）
- 截图2：`eureka-server/application.yml` 配置
- 截图3：Eureka 控制台首页显示

---

### 任务2：搭建Spring Boot网关模块(Zuul)

**实现内容**：
- 创建了 `gateway` 模块，端口 9501
- 添加了 `@EnableZuulProxy` 和 `@EnableEurekaClient` 注解
- 配置了 Zuul 路由规则，将请求转发到对应微服务
- 启用了 Ribbon 负载均衡

**Zuul 路由配置**：
```yaml
zuul:
  routes:
    # 部门服务路由
    dept-service:
      path: /dept/**
      serviceId: dept-service
    # 电影服务路由
    movies-service:
      path: /api/**
      serviceId: movies-service
    # 前端服务路由
    frontend:
      path: /**
      url: http://localhost:3002
```

**验证结果**：

| 验证项 | 结果 |
|--------|------|
| Gateway 成功注册到 Eureka | ✅ 已注册 |
| 路由转发正常 | ✅ 请求正确转发到后端服务 |
| 负载均衡生效 | ✅ Ribbon 正常工作 |

**截图说明**：
- 截图4：`GatewayApplication.java` 代码（展示 `@EnableZuulProxy` 注解）
- 截图5：`gateway/application.yml` 路由配置
- 截图6：Eureka 控制台显示 gateway 已注册

---

### 任务3：搭建Spring Boot部门微服务模块(dept-service)

**实现内容**：
- 创建了 `dept-service` 模块，端口 8001
- 添加了 `@EnableEurekaClient` 注解启用服务注册
- 实现了部门相关的 REST API 接口
- 配置了服务名称 `spring.application.name=dept-service`

**dept-service 提供的接口**：

| 接口 | 方法 | 说明 |
|------|------|------|
| `/dept/list` | GET | 获取部门列表 |
| `/dept/get/{id}` | GET | 根据ID获取部门 |
| `/dept/add` | POST | 添加部门 |

**验证结果**：

| 验证项 | 结果 |
|--------|------|
| dept-service 成功注册到 Eureka | ✅ 已注册 |
| 通过 Gateway 访问 dept-service | ✅ 路由转发正常 |
| 直接访问 dept-service | ✅ 服务本身正常 |

**截图说明**：
- 截图7：`DeptServiceApplication.java` 代码
- 截图8：`DeptController.java` 代码
- 截图9：通过 Gateway 访问 `/dept/list` 的结果

---

### 任务4：验证Zuul前置过滤器的优先级顺序

**实现内容**：
- 创建了3个自定义 Zuul 前置过滤器（Pre Filter）
- 分别设置不同的 `filterOrder` 值验证优先级
- 过滤器按 order 值从小到大顺序执行（order 越小优先级越高）

**创建的过滤器**：

| 过滤器名称 | Order 值 | 优先级 | 功能说明 |
|------------|----------|--------|----------|
| `LoggerPreFilter` | 1 | 最高 | 记录请求日志（时间、方法、URI、IP） |
| `AuthPreFilter` | 2 | 中等 | 进行权限校验，检查Authorization头 |
| `ParamCheckPreFilter` | 3 | 最低 | 参数校验，检查非法字符 |

**过滤器代码示例**：

**LoggerPreFilter.java** (Order = 1)：
```java
@Component
public class LoggerPreFilter extends ZuulFilter {
    private static final Logger logger = LoggerFactory.getLogger(LoggerPreFilter.class);

    @Override
    public String filterType() {
        return "pre";  // 前置过滤器
    }

    @Override
    public int filterOrder() {
        return 1;  // 最高优先级
    }

    @Override
    public boolean shouldFilter() {
        return true;
    }

    @Override
    public Object run() {
        RequestContext ctx = RequestContext.getCurrentContext();
        HttpServletRequest request = ctx.getRequest();

        logger.info("=== [LoggerPreFilter] 优先级1 - 请求日志记录 ===");
        logger.info("请求时间: {}", LocalDateTime.now());
        logger.info("请求方法: {}", request.getMethod());
        logger.info("请求路径: {}", request.getRequestURI());

        // 将时间戳存入上下文
        ctx.set("startTime", System.currentTimeMillis());
        return null;
    }
}
```

**AuthPreFilter.java** (Order = 2)：
```java
@Component
public class AuthPreFilter extends ZuulFilter {
    private static final Logger logger = LoggerFactory.getLogger(AuthPreFilter.class);

    @Override
    public String filterType() {
        return "pre";
    }

    @Override
    public int filterOrder() {
        return 2;  // 中等优先级
    }

    @Override
    public boolean shouldFilter() {
        return true;
    }

    @Override
    public Object run() {
        RequestContext ctx = RequestContext.getCurrentContext();
        HttpServletRequest request = ctx.getRequest();

        logger.info("=== [AuthPreFilter] 优先级2 - 认证检查 ===");

        String token = request.getHeader("Authorization");
        String uri = request.getRequestURI();

        // 放行公开路径
        if (uri.contains("/login") || uri.contains("/register")) {
            logger.info("公开路径，跳过认证: {}", uri);
            return null;
        }

        if (token == null || token.isEmpty()) {
            logger.warn("认证失败: 未找到Authorization请求头");
            ctx.setSendZuulResponse(false);
            ctx.setResponseStatusCode(401);
            ctx.setResponseBody("{\"error\": \"未授权访问\"}");
        } else {
            logger.info("认证通过");
            ctx.set("authToken", token);
        }
        return null;
    }
}
```

**ParamCheckPreFilter.java** (Order = 3)：
```java
@Component
public class ParamCheckPreFilter extends ZuulFilter {
    private static final Logger logger = LoggerFactory.getLogger(ParamCheckPreFilter.class);

    @Override
    public String filterType() {
        return "pre";
    }

    @Override
    public int filterOrder() {
        return 3;  // 最低优先级
    }

    @Override
    public boolean shouldFilter() {
        return true;
    }

    @Override
    public Object run() {
        RequestContext ctx = RequestContext.getCurrentContext();
        HttpServletRequest request = ctx.getRequest();

        logger.info("=== [ParamCheckPreFilter] 优先级3 - 参数校验 ===");

        // 获取之前过滤器存储的信息
        Long startTime = (Long) ctx.get("startTime");
        if (startTime != null) {
            long elapsedTime = System.currentTimeMillis() - startTime;
            logger.info("过滤器链执行耗时: {} ms", elapsedTime);
        }

        // 参数校验
        String param = request.getParameter("name");
        if (param != null && (param.contains("<") || param.contains(">"))) {
            logger.warn("参数校验失败: 检测到非法字符");
            ctx.setSendZuulResponse(false);
            ctx.setResponseStatusCode(400);
            ctx.setResponseBody("{\"error\": \"请求参数不合法\"}");
            return null;
        }

        logger.info("所有前置过滤器执行完成，准备转发请求");
        return null;
    }
}
```

**验证结果**：

**控制台输出顺序**（验证优先级）：
```
=== [LoggerPreFilter] 优先级1 - 请求日志记录 ===
请求时间: 2025-04-11 23:45:30
请求方法: GET
请求路径: /dept/list
客户端IP: 127.0.0.1
==============================================
=== [AuthPreFilter] 优先级2 - 认证检查 ===
认证通过，Token: Bearer eyJhbGciOiJ...
==============================================
=== [ParamCheckPreFilter] 优先级3 - 参数校验 ===
过滤器链执行耗时: 5 ms
已获取认证Token，来自AuthPreFilter
所有前置过滤器执行完成，准备转发请求
==============================================
```

| 验证项 | 预期结果 | 实际结果 | 状态 |
|--------|----------|----------|------|
| LoggerPreFilter 执行顺序 | 第1个执行 | 第1个执行 | ✅ 通过 |
| AuthPreFilter 执行顺序 | 第2个执行 | 第2个执行 | ✅ 通过 |
| ParamCheckPreFilter 执行顺序 | 第3个执行 | 第3个执行 | ✅ 通过 |
| Order 越小优先级越高 | 正确 | 正确 | ✅ 通过 |

**截图说明**：
- 截图10：`LoggerPreFilter.java` 完整代码
- 截图11：`AuthPreFilter.java` 完整代码
- 截图12：`ParamCheckPreFilter.java` 完整代码
- 截图13：控制台输出显示过滤器执行顺序
- 截图14：Postman 发送请求测试过滤器

---

## 三、运行状态截图

### 服务整体运行状态

| 服务 | 端口 | 状态 | 截图位置 |
|------|------|------|----------|
| Eureka Server | 8761 | ✅ 运行中 | 截图15 |
| Zuul Gateway | 9501 | ✅ 运行中 | 截图15 |
| Movies Service | 3001 | ✅ 运行中 | 截图15 |
| Dept Service | 8001 | ✅ 运行中 | 截图15 |
| Frontend | 3002 | ✅ 运行中 | 截图15 |

**Eureka 注册中心显示**：
- ZUUL-GATEWAY (网关，端口9501)
- MOVIES-SERVICE (电影服务)
- DEPT-SERVICE (部门服务，端口8001)

---

## 四、项目结构

```
Movies/
├── eureka-server/              # 注册中心
│   ├── src/main/java/...
│   │   └── EurekaServerApplication.java    # @EnableEurekaServer
│   ├── src/main/resources/
│   │   └── application.yml
│   └── pom.xml
├── gateway/                    # Zuul网关
│   ├── src/main/java/...
│   │   ├── MoviesGatewayApplication.java   # @EnableZuulProxy
│   │   └── filter/
│   │       ├── LoggerPreFilter.java        # Order = 1
│   │       ├── AuthPreFilter.java          # Order = 2
│   │       └── ParamCheckPreFilter.java    # Order = 3
│   ├── src/main/resources/
│   │   └── application.yml                 # 端口9501，路由配置
│   └── pom.xml
├── dept-service/               # 部门微服务
│   ├── src/main/java/...
│   │   ├── DeptServiceApplication.java     # @EnableEurekaClient
│   │   ├── controller/
│   │   │   └── DeptController.java
│   │   └── entity/
│   │       └── Department.java
│   ├── src/main/resources/
│   │   └── application.yml     # 端口8001
│   └── pom.xml
├── src/                        # 电影业务服务
│   ├── main/java/...
│   │   └── ...
│   └── main/resources/
│       └── application.yml
└── docs/                       # 文档
    ├── 数据库设计.md
    ├── 项目架构.md
    ├── 项目亮点与学习.md
    ├── 项目概述.md
    ├── 项目结构.md
    └── 实验结果.md           # 本文件
```

---

## 五、Zuul过滤器执行流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                         客户端请求                               │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Zuul Gateway (Port 9501)                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Pre Filters (前置过滤器)               │   │
│  │  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐ │   │
│  │  │LoggerPreFilter│ -> │AuthPreFilter│ -> │ParamCheckPreFilter│ │   │
│  │  │  Order = 1   │    │  Order = 2   │    │  Order = 3  │ │   │
│  │  │ 记录请求日志 │    │  权限校验    │    │  参数校验    │ │   │
│  │  └─────────────┘    └─────────────┘    └─────────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                  Routing Filter (路由过滤器)              │   │
│  │              根据路由规则转发到对应微服务                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          │                                      │
│                          ▼                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                  Post Filters (后置过滤器)                │   │
│  │              处理响应结果，添加响应头等                    │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          │               │               │
          ▼               ▼               ▼
┌─────────────────┐ ┌─────────────┐ ┌─────────────────┐
│  movies-service │ │ dept-service│ │   其他微服务     │
│    (Port 3001)  │ │ (Port 8001) │ │                 │
└─────────────────┘ └─────────────┘ └─────────────────┘
```

---

## 六、实验总结

### 完成的功能

1. ✅ **Eureka Server 搭建成功** - 端口 8761，服务注册中心正常运行
2. ✅ **Zuul Gateway 搭建成功** - 端口 9501，统一入口网关正常工作
3. ✅ **dept-service 搭建成功** - 端口 8001，微服务示例正常运行
4. ✅ **过滤器优先级验证成功** - 3个自定义过滤器按 Order 值顺序执行

### 遇到的问题与解决

| 问题 | 解决方案 |
|------|----------|
| Zuul 过滤器不生效 | 确保过滤器类添加了 `@Component` 注解被 Spring 管理 |
| 过滤器执行顺序不符合预期 | 检查 `filterOrder()` 返回值，数值越小优先级越高 |
| 请求上下文传递失败 | 使用 `RequestContext.getCurrentContext()` 获取上下文 |

### 实验总结

通过本次实验，深入理解了 Spring Cloud Zuul 网关在微服务架构中的核心作用。Zuul 作为微服务架构的统一入口，承担着路由转发、权限校验、日志记录等重要职责。通过搭建 Eureka 注册中心、Zuul 网关和 dept-service 微服务，构建了一个完整的微服务调用链路。

在过滤器实验中，创建了3个具有不同优先级的自定义前置过滤器（LoggerPreFilter、AuthPreFilter、ParamCheckPreFilter），通过设置不同的 `filterOrder` 值（1、2、3），验证了 Zuul 过滤器按 Order 值从小到大顺序执行的机制。Order 值越小，过滤器优先级越高，越早执行。这一机制使得开发者可以精确控制过滤器的执行顺序，实现如先记录日志、再校验权限、最后统计访问等有序处理流程。

通过本次实践，掌握了 Zuul 过滤器的开发方法，理解了过滤器在请求处理流程中的位置，为后续实现更复杂的网关功能（如统一认证、限流熔断、灰度发布等）打下了坚实基础。

---

## 七、参考文档

- [EUREKA-SETUP.md](../EUREKA-SETUP.md) - Eureka 微服务启动指南
- Spring Cloud Netflix Zuul 官方文档
- Spring Cloud Gateway 官方文档
